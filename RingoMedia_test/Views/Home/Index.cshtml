@model IEnumerable<Department>

<h2>Departments</h2>
<ul>
    @foreach (var department in Model)
    {
        <li>
            <a href="@Url.Action("Index", new { id = department.DepartmentId })">
                @department.DepartmentName
            </a>
        </li>
    }
</ul>
